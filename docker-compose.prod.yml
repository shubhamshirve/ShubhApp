# ============================================================================
# eBill Docker Compose - Production Environment
# ============================================================================
# Features:
#   - Optimized resource limits
#   - Production-grade logging
#   - Security-focused configuration
#   - No debug endpoints
#   - Automatic restart on failure
# ============================================================================

services:
  # Initialize critical environment variables
  init-env:
    image: alpine:3.20
    restart: "no"
    working_dir: /app
    environment:
      DOMAIN: ${DOMAIN}
      SERVER_IP: ${SERVER_IP}
      MONGO_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      BACKUP_PASSWORD: ${BACKUP_PASSWORD}
      CORS_ORIGINS: ${CORS_ORIGINS}
      REACT_APP_BACKEND_URL: ${REACT_APP_BACKEND_URL}
    command: ["/bin/sh", "/app/docker/init-env.sh", "/app"]
    volumes:
      - ./:/app
    networks:
      - ebill
    deploy:
      resources:
        limits:
          cpus: "0.2"
          memory: 64m

  # MongoDB - Database (Production)
  mongodb:
    image: mongo:4.4
    restart: always
    depends_on:
      init-env:
        condition: service_completed_successfully
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
    # Limit WiredTiger cache = 15% of total RAM (set by 'make auto-config')
    # Default: 0.15GB (safe for 1GB VPS). Run 'make auto-config' after RAM upgrade.
    command: mongod --wiredTigerCacheSizeGB ${MONGO_CACHE_GB:-0.15}
    ports:
      - "127.0.0.1:27017:27017"  # Bind to localhost only, accessed via backend on same network
    volumes:
      - mongo_data:/data/db
    networks:
      - ebill
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongo localhost:27017/test --quiet
      interval: 30s
      timeout: 30s
      retries: 5
      start_period: 90s
    deploy:
      resources:
        limits:
          cpus: "0.8"
          memory: ${MONGO_MEMORY_LIMIT:-300m}
        reservations:
          cpus: "0.1"
          memory: ${MONGO_MEMORY_RESERVATION:-150m}
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "10"

  # FastAPI Backend (Production)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        BUILD_ENV: production  # Use requirements.txt (production only)
    image: ${DOCKER_USERNAME:-teasyservices}/ebill-backend:${VERSION:-latest}
    restart: always
    depends_on:
      init-env:
        condition: service_completed_successfully
      mongodb:
        condition: service_healthy
    environment:
      DB_NAME: ${DB_NAME:-saas_db}
      # Authenticated MongoDB URI for production
      MONGO_URI: mongodb://${MONGO_ROOT_USERNAME}:${MONGO_ROOT_PASSWORD}@mongodb:27017/${DB_NAME:-saas_db}?authSource=admin
      MONGO_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
      CORS_ORIGINS: ${CORS_ORIGINS}
      JWT_SECRET: ${JWT_SECRET}
      BACKUP_PASSWORD: ${BACKUP_PASSWORD}
    expose:
      - "8000"
    networks:
      - ebill
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s
    deploy:
      resources:
        limits:
          cpus: "0.8"
          memory: ${BACKEND_MEMORY_LIMIT:-256m}
        reservations:
          cpus: "0.1"
          memory: ${BACKEND_MEMORY_RESERVATION:-128m}
    labels:
      - "com.example.description=FastAPI backend service"
      - "com.example.version=${VERSION:-latest}"
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "10"

  # React Frontend (Production)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        REACT_APP_BACKEND_URL: ${REACT_APP_BACKEND_URL}
    image: ${DOCKER_USERNAME:-teasyservices}/ebill-frontend:${VERSION:-latest}
    restart: always
    depends_on:
      backend:
        condition: service_healthy
    expose:
      - "3000"
    networks:
      - ebill
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/index.html"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: ${FRONTEND_MEMORY_LIMIT:-200m}
        reservations:
          cpus: "0.05"
          memory: ${FRONTEND_MEMORY_RESERVATION:-100m}
    labels:
      - "com.example.description=React frontend service"
      - "com.example.version=${VERSION:-latest}"
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "10"

  # Caddy Web Server (Production)
  caddy:
    image: caddy:2.8-alpine
    restart: always
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - ebill
    environment:
      DOMAIN: ${DOMAIN}
      SERVER_IP: ${SERVER_IP}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: ${CADDY_MEMORY_LIMIT:-64m}
        reservations:
          cpus: "0.05"
          memory: ${CADDY_MEMORY_RESERVATION:-32m}
    labels:
      - "com.example.description=Caddy reverse proxy (production)"
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "10"

volumes:
  mongo_data:
    driver: local
  caddy_data:
    driver: local
  caddy_config:
    driver: local

networks:
  ebill:
    driver: bridge
